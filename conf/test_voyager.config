/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for testing Voyager failure isolation and MultiQC custom content
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

process {
    resourceLimits = [
        cpus: 4,
        memory: '15.GB',
        time: '1.h'
    ]

    withName: MINIMAP2_ALIGN {
        cpus = 2
        memory = '8.GB'
        time = '1.h'
    }

    withName: VOYAGER_PROFILE {
        cpus = 1
        memory = '4.GB'
        time = '1.h'
    }
}

params {
    config_profile_name        = 'Voyager failure-isolation test profile'
    config_profile_description = 'Small test dataset with an invalid Voyager index; the pipeline must still complete.'

    input = "${projectDir}/assets/test_sylph_samplesheet.csv"
    fasta = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/genomics/homo_sapiens/genome/genome.fasta'
    skip_kraken2 = true
    skip_sylph = true
    skip_voyager = false
    voyager_db = "${projectDir}/assets/test_voyager_invalid.idx"
}
